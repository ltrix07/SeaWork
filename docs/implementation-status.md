# Состояние реализации

> Отображение разделов `SPEC.md` на код. Спека описывает **чем продукт является** и намеренно не содержит отметок о выполнении: замысел и состояние реализации меняются с разной скоростью, и смешивать их в одном файле — значит потерять оба.
>
> Обновляется при закрытии вехи. Последнее обновление: 22.09.2026 — сверка с кодом по ходу вехи 2, после того как цифры здесь разошлись с тестами (в документе стояли 251 вакансия и два источника при 605 и трёх).

---

## 1. Сводка

Реализован ingestion-конвейер: этапы 1–5 из десяти, описанных в SPEC.md §9.2. Пользовательская часть продукта — рекомендации, тарифы, онбординг, бот — не начиналась.

| Слой | Состояние |
|---|---|
| Сбор и хранение сырых данных | работает, три источника, 605 вакансий |
| Нормализация, качество, классификация | работает |
| Обогащение правилами | работает, потолок измерен |
| Обогащение LLM | измерено 22.09.2026 на `gpt-5.4-mini`: порог по ложным заполнениям взят, ограничитель — полнота |
| Match Score | не начат |
| Всё пользовательское | не начато |

---

## 2. Recommendation Pipeline (SPEC.md §9)

| Этап спеки | Состояние | Где |
|---|---|---|
| §9.3 Этап 1. Получение возможности | **готово** | `ingestion/sources/`, `ingestion/pipeline.py` |
| §9.4 Этап 2. Нормализация | **готово** | `Source.normalize()` в каждом адаптере |
| §9.5 Этап 3. Проверка качества | **готово** | `ingestion/quality.py` |
| §9.6 Этап 4. Определение типа | **готово** | `ingestion/classify.py` |
| §9.7 Этап 5. Обогащение | **готово для двух полей** | `enrich/rules.py` + `enrich/llm.py`; модель выбрана, см. §4 |
| §9.8 Этап 6. Match Score | не начат | — |
| §9.9 Этап 7. Ограничения подписки | не начат | — |
| §9.10 Этап 8. История пользователя | не начат | — |
| §9.11 Этап 9. Группировка | не начат | — |
| §9.12 Этап 10. Формирование рекомендации | не начат | — |

Этапы 6–10 требуют профиля пользователя, которого пока нет ни в каком виде.

---

## 3. Что даёт конвейер сегодня

Три источника — Pinpoint (`hollandamericagroup` 93, `princesscruises` 354) и Crewplanet (158), **605 вакансий** в снапшотах. Покрытие зафиксировано тестами (`tests/test_snapshot.py`, `tests/test_crewplanet.py`), проценты ниже считаны по корпусу целиком:

| Поле | Заполняемость | Комментарий |
|---|---:|---|
| `title`, `description`, `url`, `type` | 100% | |
| `profession` | 98.8% | 598/605 |
| `required_languages` | 57.2% | 346/605, введено в B8 |
| `experience_level` | 39.2% | 237/605 |
| `required_certificates` | 33.2% | 201/605 |
| `salary` | 26.1% | 158/605: Crewplanet даёт всем, Pinpoint не даёт вовсе |
| `posted_at` | 26.1% | те же 158 — Pinpoint не даёт дат в принципе |
| `preferred_languages` | 0.3% | 2/605; сигнал «приветствуется» в текстах редок, низкая цифра ожидаема |
| `country`, `city` | **0%** | см. §5 — это свойство предметной области |

**Опыт и сертификаты просели после подключения Princess** — было 45.0% и 48.2% на двух источниках. Это не регрессия правил: Princess формулирует требования иначе, и правила его формулировок не читают. Ровно этот провал и должен закрыть блок D.

Команды: `seawork ingest run|refetch|reprocess`, `seawork report coverage`, `seawork eval enrichment --provider X [--show-errors]`, `seawork db upgrade`.

---

## 4. Ближайшая работа

Полный список задач с зависимостями — `docs/roadmap.md`. Веха 2 — **расширение источников**: цель ближайшего периода в полноте и качестве данных, а не в видимом прототипе.

**Следующая задача — D3, прогон провайдеров.** `LLMEnricher` (`enrich/llm.py`), персистентный кэш ответов и команда `seawork eval enrichment` написаны и покрыты тестами; эталон вырос до 75 записей и проходит `scripts/check_eval_gold.py`. Не сделан последний шаг: `llm_provider` не настроен, ни один живой провайдер не прогонялся, цифр по качеству нет. До этого прогона неизвестно, закрывает ли LLM провал по опыту (39.2%) и сертификатам (33.2%) — то есть у нас есть качество кода и нет свидетельств качества результата. Порог подключения — доля ложных заполнений не выше 5%.

**Новые источники.** PADI (`divejobs.padi.com`) — дайвинг и `INTERNSHIP`, которых в базе нет вовсе. Структурной разметки там нет (проверено 20.07.2026: ноль блоков `ld+json`; более раннее утверждение о JSON-LD было ошибкой переноса), `padijobs/v1/jobs` отдаёт 401 — значит обычный разбор HTML. Разведка ATS-платформ (B9) показала, что отрасль универсальными ATS в массе не пользуется: расширение «бесплатно, через платформу» упёрлось в потолок.

**Поля географии** — `embarkation_port_raw` и `operating_region` (см. §5). Блок A не начат: полей нет ни в модели домена, ни в справочниках, и `data/reference/ports.yaml` с `regions.yaml` не созданы. Пока это так, географического фильтра у продукта нет никакого.

**Долги качества.** `tests/test_storage.py` отсутствует — хранилище единственный полностью непокрытый слой. `scripts/audit_payload_coverage.py` и `scripts/check_eval_gold.py` возвращают коды выхода, но в CI не вызываются: дефект с потерей данных, найденный в вехе 1 вручную и по случайности, ничем не защищён от повторения.

---

## 5. Решения, принятые в обход спеки

Места, где реализация расходится с текстом SPEC.md или уточняет его. Часть требует продуктового решения при возврате к соответствующему разделу, часть уже решена — состояние указано у каждого пункта.

**География судовых вакансий (§9.4, §10) — решено 20.07.2026.** У судового контракта нет страны работы в береговом смысле: все три источника дают 0%, и это свойство отрасли, а не дефект парсинга. Фильтрация по стране, подразумеваемая моделью рекомендаций, для таких вакансий невозможна в принципе.

Замена — **порт посадки и регион работы**: два разных поля, `embarkation_port_raw` и `operating_region`. Это ближе к тому, как моряки выбирают контракт на самом деле, чем страна.

Реализация — блок A в `docs/roadmap.md`. Открытым остаётся поведение этих полей при появлении берегового источника: общая схема или отдельные ветки (задача A1).

**Граница инфраструктурного и тарифицируемого AI (§3.4, §3.7).** Проведена в `docs/ai-cost-boundary.md`. Там же зафиксировано расхождение: спека относит перевод вакансий к VIP, но по принятому правилу перевод при загрузке — инфраструктура и должен быть доступен всем.

**Курсы и сертификации (§4.3).** Классифицированы как near-static каталог, который выгоднее вести вручную, чем парсить. Обоснование — `docs/source-registry.md`.

**Многоязычность корпуса — отложено сознательно.** 4.9% вакансий Crewplanet написаны по-русски; правила извлечения англоязычные и на них не срабатывают. Спека предполагает русскоязычную аудиторию (§2.2), но о языке исходных данных не говорит. Расширение правил (задача C3) отложено до результатов оценки LLM: возможно, модель закроет эти записи сама, и правка окажется лишней.

---

## 6. Чего в спеке нет, а в реализации потребовалось

- **`first_seen_at`** — момент первого обнаружения объекта. Pinpoint не отдаёт дат публикации вовсе, и без этого поля невозможна сортировка по свежести (`docs/modules/source-pinpoint.md` §3.2). Это наш факт, а не факт источника.
- **`recruitment_office_raw`** — офис найма, отделённый от места работы. Без него `location` круизных источников попадал бы в `country`/`city` и порождал бы уверенно неверный Match Score (§3.1 там же).
- **`provenance` и `confidence` у каждого выведенного поля** — `domain/inferred.py`. Спека требует объяснимости Match Score (§10.7), но не говорит, откуда берётся основание для объяснения.
- **`source_trust`** — доверие к источнику, независимое от его структурности. Агрегатор с чистым API может быть менее достоверен, чем сайт работодателя с HTML.
- **Таблица кэша LLM-ответов** (миграция `0002_llm_cache`). Спека требовала кэш по ключу `content_hash` × `prompt_version` × `model_id`, но не говорила, где он живёт. В памяти процесса он бесполезен: `reprocess` запускается отдельным процессом, и платить за те же payload'ы пришлось бы заново.
